# 能源知识库建设计划

## RTU文章图形化改版 `complete`

### 范围与分层

- 把现有RTU总架构图移到文章标题之后，使读者进入页面首先获得整体认识。
- 新增“遥控闭环”和“I/O信号分类”两张交互图，并压缩重复的解释文字，形成“总览图—流程图—分类图—短说明”的阅读顺序。
- 图形层使用版本化JSON与自包含HTML；文章层仅通过iframe消费图，不重复实现图形逻辑；发布仍走VitePress与GitHub Pages。

### 验收标准

- [x] 总架构图位于正文最前面、首段解释之前。
- [x] 正文至少有三张图，分别解释总体结构、控制闭环和I/O分类。
- [x] 删除与图中信息重复的大段文字，保留安全边界、定义和必要选型说明。
- [x] 三张图通过showcase验证、桌面浏览器证据与人工视觉检查。
- [x] 文章以桌面端图形阅读为主，三张iframe可加载且没有页面横向溢出；移动端只保证基本滚动和全屏入口，不为小屏牺牲图的信息密度。
- [x] 构建、PR、Pages部署和正式URL复验通过。

### 兼容与恢复

- 不改变文章URL、既有导航或站点运行时；回滚本次文章和新增演示文件即可恢复上一版。
- JSON规范、HTML图和Markdown均进入Git，干净环境可用既有构建命令恢复。

### 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 遥控闭环图在1440px桌面投影后最小上下文字号为5.81px | 1 | 按诊断把viewBox宽度从1120缩至1040，复验可读性通过 |
| I/O分类图的AI标签与DI连线路径间距为0 | 1 | 仅对AI关系使用`labelDx: -50`，复验标签净距11.4px |
| 遥控闭环图首次浏览器检查出现垂直溢出 | 1 | 移除与箭头重复的三张说明卡和参与者副标题，改用1200×620宽画布 |
| 压缩画布后末条消息和状态分段超出时间轴边界 | 1 | 同步上移消息、分段与激活区间，复验showcase与四档桌面视口均通过 |

## RTU基础文章发布 `complete`

### 范围与分层

- 新增一篇面向初学者的工业控制知识文章，讲清RTU是什么、负责什么、由哪些部分组成，以及它和I/O、数采、现场设备、站控与调度系统的关系。
- 概念层先建立“现场—RTU—站控/调度”的主线；信号层解释DI、DO、AI、AO、PI；架构层解释南向接入、主控处理与北向通信。
- 展示层复用本轮已生成并验证的RTU架构HTML，作为版本化站内演示；正文即使不加载演示也能独立读懂。
- 发布层仅消费Git中的Markdown、HTML和导航配置，由现有VitePress与GitHub Pages流程构建。

### 验收标准

- [x] 正文按“问题—定义—工作流程—功能—结构—接口—边界”组织，避免术语堆砌。
- [x] 明确一个I/O通道对应一个信号点，而非一台设备，并解释脉冲量与模拟量为何分开处理。
- [x] 从首页、顶部导航或工业控制侧边栏可以到达文章。
- [x] 架构演示可在站内加载，正文、导航和演示在桌面端与移动端可读。
- [x] `npm run check`、`git diff --check`和恢复验证通过。
- [x] 变更通过PR合入`main`，Pages发布成功并复验正式URL。

### 兼容、恢复与跟踪

- 内容型增量，不修改现有文章URL、站点运行时或批阅接口；回滚时撤销文章、演示文件和导航入口即可。
- GitHub仓库是正文、演示和配置的权威来源；干净环境用`npm ci && npm run check`重建验证。
- 当前会话未提供Linear写入工具，范围、决策和验证证据先记录在仓库计划文件中。

### 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 浏览器新建EnergyBook标签页和直接导航连续超时 | 2 | 根据现有生产页响应头确认站点由GitHub Pages发布，转用权威Git仓库工作流，不重复失败路径 |
| 公网搜索未识别用户的EnergyBook站点 | 1 | 从已有同域演示标签页和站点响应确认正式域名 |
| 更新计划文件时补丁上下文未匹配 | 1 | 读取当前RTU计划段落后使用精确上下文更新，没有影响文章或构建产物 |
| `npm ci`报告1项中等级和1项高等级开发依赖告警 | 1 | 本次未修改锁文件或依赖；构建与23项回归测试通过，依赖升级留给独立维护任务 |
| Modbus官方规范索引对命令行链接检查返回HTTP 403 | 1 | 已由Modbus Organization网页读取器确认页面与规范内容；判断为反自动化策略，保留官方链接并如实记录 |
| PR构建提示GitHub Actions的Node.js 20执行环境已弃用 | 1 | 运行器自动强制使用Node.js 24且构建通过；属于既有工作流维护项，本次内容发布不修改CI基础设施 |
| 正式页面检查脚本使用了zsh保留数组名`path`，导致该子shell内PATH失效 | 1 | 未重复原命令；改用任务专用变量名重新检查，四个正式URL及正文标记全部通过 |

## HubPort三产品资料研究与发布 `complete`

### 范围与分层

- 分别发布系统集成方案、工业软网关和设备集成方案三篇文章，不把三类对象合并为单一产品。
- 证据层只使用HubPort官方产品页、官方中英文PDF、白皮书和服务说明；文章层区分厂商主张、工程解释与尚待验证事项。
- 展示层将六张官方PDF原页作为版本化图片嵌入正文，同时附官方PDF直链，便于读者查看和下载完整原始资料。
- 发布层只消费Git中的Markdown和图片，由现有VitePress与GitHub Pages流程构建；不依赖生成时的临时文件。

### 验收标准

- [x] 三个产品/方案各有独立文章、独立路由和首页/导航入口。
- [x] 每篇均附中文与英文官方PDF、官方产品页、白皮书，并嵌入对应官方PDF原页截图。
- [x] 解释能力模型、驱动生成与Runtime关系，明确采集、写入控制、既有数采复用和关键控制边界。
- [x] `npm run check`、`git diff --check`和原始资料链接检查通过。
- [x] 变更经PR合入`main`，Pages发布成功，三篇正式页面、图片和sitemap复验通过。

### 兼容、恢复与项目跟踪

- 内容型增量，不改变既有路由、组件、数据契约或运行时依赖；回滚可撤销文章、图片和入口，由Pages从Git重新构建。
- 官方PDF保留厂商原始链接，六张引用页截图进入Git，避免文章关键证据只存在于本机缓存。
- 当前会话未提供Linear写入工具；范围、验收和验证证据记录在仓库计划文件中。

## 场站 V/F/P/Q 动画发布

- 范围：固定原版源码版本，导出站内自包含 HTML，新增知识文章和工业控制导航。
- 分层：原版 React 模型与视图 → 静态打包器 → iframe 文章 → GitHub Pages；文章不重绘、不重复实现模型。
- 验收：原版文件哈希一致；静态导出成功；知识库检查通过；PR 合并后 Pages 成功且文章/动画可访问。
- 依赖和恢复：源码快照、锁文件和导出命令进入版本库；构建产物可重新生成，回滚使用正常 PR。
- 状态：导出及构建验证通过，等待 PR 和 Pages 发布。

## 阶段 3.7：补充构网型微电网厂商满足度 `complete`

### 范围

- 在既有《构网型微电网控制器洞察》中增加统一系统要求下的厂商公开证据满足度。
- 比较施耐德、西门子、SEL、华为、阳光电源和日立能源，覆盖构网电源、状态转换、多源协调、能量管理、保护协同、开放接入及测试/版本证据。
- 明确“公开证据满足度”不等于现场性能排名，未公开不等于不具备。
- 沿用现有文章 URL、导航和 GitHub Pages 发布链路，不新增运行时功能。

### 分层与依赖边界

1. 要求层：用同一组系统问题定义评价口径，不采用厂商自定义指标直接横比。
2. 证据层：优先使用当前厂商官方产品页、白皮书和具名项目；厂商材料只证明公开主张。
3. 洞察层：给出场景化适配判断，不形成缺少统一试验支撑的总分榜。
4. 展示与发布层：只修改版本化 Markdown 和计划记录，由现有 VitePress/GitHub Pages 构建发布。

### 验收标准

- [x] 七项系统要求、评级图例和六家厂商矩阵口径一致。
- [x] 每家厂商的强项、边界和适用场景具有官方来源依据。
- [x] 明确当前尚不能证明的三项系统级能力和采购证据包要求。
- [x] `npm run check`、`git diff --check`及文章构建产物检查通过。
- [x] 变更通过 PR 合入 `main`，GitHub Pages 成功发布并复验正式文章。

### 兼容与回滚

- 内容型增量，不改变文章路由、导航、组件、数据契约或运行时依赖。
- 回滚时恢复该 Markdown 和三份计划文件到上一版本，由 GitHub Pages 重建即可。

### 项目跟踪

- 继续归入 Linear TIN-414；当前会话未提供 Linear 写入工具，仓库计划文件先记录本次范围和状态。

### 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 施耐德两条官方链接对命令行自动请求返回HTTP 403 | 1 | 改用网页读取器复核；产品页和18页官方PDF均可读取，确认是反自动化策略而非链接失效 |
| Pages监视命令在一次输出续接时返回不可用的执行单元标识 | 1 | 不重复等待失效标识，改用`gh run view`按运行ID轮询，确认构建和部署均成功 |

## 阶段 3.6：发布构网型微电网控制器洞察 `complete`

### 范围

- 将 ForesightScope 当前约 250 条有效证据整理为一篇面向控制器软件产品决策的详细洞察报告。
- 重点回答行业阶段、买方需求、友商路线、控制器软件产品含义、证据充分度和后续监测重点。
- 明确区分直接事实、跨来源推断和当前不能支持的结论，并提供代表性原始来源。
- 将文章加入首页、顶部导航和侧边栏，并通过既有 GitHub Pages 流程发布。

### 分层与依赖边界

1. 证据层：只提供可追溯事实和样本统计，不依赖 EnergyBook 展示代码。
2. 洞察层：基于证据形成推理链并声明边界，不把厂商自述当作独立验证。
3. 展示层：VitePress Markdown、首页和导航只消费文章内容，不引入采集逻辑或运行时服务。
4. 发布层：只消费 Git 中的版本化内容；禁止在生产站点手工修改生成文件。

### 验收标准

- [x] 核心判断具有“事实→变化→产品含义”的完整推理链。
- [x] 厂商、买方采购和独立测试三类证据均有代表性依据与来源链接。
- [x] 友商分析是路线分类而非无依据排名，资料口径和时效边界清晰。
- [x] 正式文章可从首页、顶部导航和侧边栏访问。
- [x] `npm run check`、`git diff --check`和移动端/桌面端页面验收通过。
- [x] 变更经 PR 合入 `main`，GitHub Pages 发布成功，正式 URL 和 sitemap 复验通过。

### 兼容与回滚

- 内容型增量，不修改既有文章 URL、主题运行时、批阅接口或数据契约。
- 回滚时删除新增文章并移除三个导航入口；Git 中的上一版本可通过既有 Pages 流程重建。

### 项目跟踪

- Linear：TIN-414，High，In Progress。

### 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 华为案例和日立PPC原厂引用路径返回HTTP 404 | 1 | 改用已验证返回200的ForesightScope稳定收录页，并在来源说明中明确原厂路径失效 |
| `npm ci`报告1项中等级开发依赖告警 | 1 | 本次未修改依赖；记录为既有风险，不在内容发布中扩大到依赖升级 |
| 本地开发页首次加载请求`/favicon.ico`返回404 | 1 | 站点已配置并正常加载`logo.svg`图标，文章、导航和生产构建不受影响；保留给全站图标维护任务，不在内容发布中增加重复图标资产 |
## 阶段 3.8：发布继电保护架构演进文章与五代动画 `complete`

### 范围

- 将本轮讨论整理为一篇连续的继电保护架构演进文章，讲清机电式、静态式、微机式、数字网络化以及虚拟协同阶段的来龙去脉。
- 为五个阶段分别提供可播放、单步和复位的控制过程动画，并在关键位置展示点对点、过程总线、集中式和虚拟化拓扑。
- 将文章、动画、导航和引用加入EnergyBook，沿用现有VitePress与GitHub Pages发布流程。

### 分层与依赖

1. 文章层：历史、架构变化、控制过程、适用边界和演进方向。
2. 演示层：独立HTML动画；文章在JavaScript失效时仍保留完整文字解释。
3. 导航层：首页或知识导航入口，保持既有URL和主题组件兼容。
4. 发布层：Git `main`、GitHub Actions和Pages；不增加服务器运行时状态。

### 验收标准

- [x] 文章从站内导航可达，正文清楚区分保护装置与断路器。
- [x] 五代动画均可播放、暂停、单步和复位，并展示各自的测量—判断—跳闸过程。
- [x] 桌面端和移动端无横向溢出，深浅主题可读，并提供减弱动画支持。
- [x] 关键标准与当前技术方向具有可追溯来源，明确“第五代”并非统一正式标准名称。
- [x] `npm run check`与`git diff --check`通过。
- [x] 变更进入`main`，GitHub Pages部署成功，生产文章和动画返回HTTP 200。

### 回滚与恢复

- 回退本次发布提交即可移除文章、动画与导航入口，不改变既有内容URL和发布基础设施。
- GitHub仓库仍为正文、动画和配置的权威源；干净环境使用`npm ci && npm run check`验证恢复。

### 已知错误

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 浏览器打开EnergyBook时两次等待超时 | 2 | 已用站点响应头和本地项目记录确认站点由GitHub Pages发布，改走权威Git仓库发布路径，不重复相同浏览器导航 |
| 首次`npm run check`缺少本地`vitepress`可执行文件 | 1 | 测试23/23通过；按锁文件运行`npm ci`恢复开发依赖后重新执行完整检查 |
| 首次只进入外层动画iframe，读取内层控件超时 | 1 | 确认演示文件使用双层受限iframe，改用嵌套frame定位后逐代验证成功 |
| 自动化鼠标点击未改变双层iframe内按钮状态 | 1 | 未用脚本直接改状态；改以按钮键盘激活执行真实事件，五代切换、单步、播放、暂停和复位均通过 |
| 远端`main`在实施期间新增15个提交，重放时五个共享文件冲突 | 1 | 保留远端新增内容并合并本次导航、样式和计划记录，将本阶段编号顺延为3.8后重新构建 |
| 干净副本首次用`npm --prefix`安装时误把临时目录名当包名 | 1 | 核对源码与锁文件名称一致，进入副本目录直接执行`npm ci && npm run check`后恢复演练通过 |
| 正式内容检查使用了当前环境不存在的Homebrew `rg`绝对路径 | 1 | 保留已成功的四个HTTP 200结果，改用当前应用内置`rg`完成正文、首页和sitemap检查 |
| 正式文章的浏览器导航超过30秒并重置会话 | 1 | 不重复同一路径；生产文章、动画、首页和sitemap均为HTTP 200，正文与路由内容检查通过，本地同构输入已完成全交互矩阵 |
## 阶段 3.5：发布近十年华为系新能源创业观察 `complete`

### 范围

- 将已核验的近十年人物表格和简要结论整理为EnergyBook行业洞察。
- 只保留2016年8月4日至2026年8月4日内成立的5家入选公司，不加入排除公司或证据可靠性分级。
- 将文章加入首页、顶部导航和侧边栏。

### 验收标准

- [x] 正式文章可从首页和导航访问。
- [x] 人物、公司、华为履历、成立时间、赛道和截至2026年8月的进展完整呈现。
- [x] 主要资料来源可追溯。
- [x] `npm run check`与`git diff --check`通过。
- [x] 变更通过PR合入`main`并完成GitHub Pages生产验证。

### 回滚

回退本次发布提交即可删除文章及三个导航入口，不改变既有文章URL、主题组件或发布流程。

### 项目跟踪

- Linear读取能力正常，但当前会话未提供创建事项工具；范围、验收、验证与回滚先完整记录在仓库计划文件中。

## 阶段 3.3：发布《欧洲能源的两个机会：储能与微网》 `complete`

### 范围

- 将已完成并核验的储能与微网文章整理为EnergyBook正式行业洞察。
- 保留数据来源、市场测算、适用边界和公式。
- 将文章加入首页、顶部导航和侧边栏。

### 验收标准

- [x] 正式文章可从首页和导航访问。
- [x] 14个来源链接、核心数字与适用边界完整保留。
- [x] `npm run check`与`git diff --check`通过。
- [x] 变更进入`main`并完成GitHub Pages生产验证。
- [x] Linear受限原因与仓库验证证据、回滚说明均已记录。

### 回滚

回退本次发布提交即可恢复上一版站点；GitHub `main`分支是内容与配置的权威来源。

### 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| Linear工作区超过免费事项数量上限，无法创建发布事项 | 1 | 不重复创建；在仓库计划、发现和进度文件中保留完整范围、验收、验证与回滚记录 |
| 推送`main`时远端已出现本地尚未包含的新提交 | 1 | 停止推送；获取并审查远端差异，rebase安全整合后重新运行完整检查并成功推送 |

## 阶段 3.2：发布欧洲能源产业2026—2030洞察 `complete`

### 范围

- 将已完成的简明洞察整理为EnergyBook正式文章。
- 保留核心判断、关键数据、推导、华为机会、控制器软件机会和原始资料索引。
- 将文章加入首页、顶部导航和侧边栏。

### 分层与依赖

1. 洞察文章：事实、判断、预测边界和原始来源。
2. VitePress导航：首页卡片、顶部入口和侧边栏入口。
3. 发布层：沿用现有GitHub Pages工作流，不新增运行时或服务端依赖。

### 验收标准

- [x] 正式文章可从首页和导航访问。
- [x] 所有核心数据保留原始机构链接，厂商自述明确标注。
- [x] 正文简洁，事实、判断和预测不混淆。
- [x] `npm run check`通过。
- [x] 合并到主分支并完成GitHub Pages生产验证。

### 回滚与兼容

- 不改变既有文章URL、主题组件或批阅接口。
- 回滚时删除新文章及三个导航入口即可。

### 错误记录

| 错误 | 尝试 | 处理 |
|---|---|---|
| Linear工作区超过免费事项数量上限 | 创建本次独立内容发布事项 | 使用仓库计划文件记录范围、验收、验证和回滚，继续发布 |
| 生产URL检查脚本误用zsh特殊变量`path`，导致命令搜索路径被覆盖 | 首次生产URL检查 | 改用`url_path`变量后重新检查，三个生产地址均返回HTTP 200 |

## 总体目标

建设一个托管在 GitHub、通过 GitHub Pages 公开展示的中文能源知识库。内容以 Markdown 为单一信息源，支持搜索、导航、引用、图表和可复用交互动画，并具备清晰的编辑、审核和发布流程。

## 建设原则

- Markdown 优先：文本离线可读、方便 diff 和审核。
- 素材与内容分离：动画、样式、数据文件集中管理，文章只声明如何使用。
- 渐进增强：动画失效时仍有文字、图片或表格可读。
- 证据可追溯：重要结论标注来源、发布时间、统计口径和访问日期。
- 先建小而稳定的底座，再扩展内容、数据和可视化。

## 阶段

### 阶段 1：架构与可运行底座 `complete`

- [x] 检查工作区、Git 状态和现有约束
- [x] 创建持久化计划、发现和进度文件
- [x] 核对当前 GitHub Pages 和静态站点生成器的官方部署方式
- [x] 完成技术选型与目录规范
- [x] 搭建首页、导航、示例文章和可复用动画
- [x] 配置本地构建、基础质量检查和 GitHub Pages 发布
- [x] 验证构建产物与关键页面

### 阶段 1.1：迁移到 VitePress `complete`

- [x] 核对 VitePress 对 Markdown、Vue 组件、原始 HTML、静态资源和 GitHub Pages 的官方支持
- [x] 将站点配置、主题、导航和搜索迁移到 VitePress 1.6.4
- [x] 将能量流 Web Component 迁移为 SSR 友好的 Vue 组件
- [x] 建立 `public/demos/` 独立 HTML 动画规范
- [x] 更新 GitHub Pages 工作流并验证生产构建与页面

### 阶段 2：内容体系与编写规范 `in_progress`

- [ ] 定义能源领域分类法、标签和文章模板
- [ ] 定义引用、数据口径、图表和术语表规范
- [x] 建立交互动画编写与验收规范初稿
- [ ] 建立贡献指南、审核清单和 issue/PR 模板
- [ ] 确定首批主题及负责人

### 阶段 2.1：在线登录、批阅与自动改稿 `complete`

- [x] 确定公开阅读、登录后批阅的产品形态
- [x] 确定自托管 n8n → Codex Cloud → GitHub 的执行架构
- [x] 定义文章与动画代码的修改白名单
- [x] 在 VitePress 中加入登录状态、文字划选和批阅面板
- [x] 建立 GitHub OAuth 登录与短期会话交换
- [x] 实现短期会话网关、OAuth state/PKCE、GitHub 仓库写权限校验、CORS 和提交限流
- [x] 建立并发布 n8n 批阅接收、任务追踪和错误处理工作流
- [x] 在 `molt` 建立低权限 Codex Cloud 执行器
- [x] 加入文件范围、安全扫描和构建门禁代码
- [x] 接通临时分支、PR 与自动合并门禁
- [x] 为 READY 任务增加持久化发布器：应用 Cloud diff、策略复检、推送受限分支
- [x] 配置仅绑定 `energy-handbook` 的写入 Deploy Key，并完成真实任务端到端合并
- [x] 完成端到端验证并发布

### 阶段 2.2：批阅清单与批量提交 `complete`

- [x] 将单条批阅抽屉改为“加入清单”，支持查看、删除和跨页面暂存
- [x] 一次提交最多 20 条批注，只创建一个 Cloud Codex 任务和一个 PR
- [x] 服务端兼容旧单条负载，并对批量条数、单条长度和总请求大小执行校验
- [x] 更新测试、使用文档并完成浏览器与端到端部署验证

### 阶段 2.3：站内失效链接巡检 `in_progress`

- [x] 抓取线上页面的站内导航、正文链接、iframe 与静态资源地址
- [x] 区分 GitHub Pages 子路径错误、缺失文件和客户端路由错误
- [ ] 修复确认失效的链接并增加自动化链接门禁
- [ ] 完成线上点击回归与 Pages 部署验证

### 阶段 2.4：专注阅读模式 `complete`

- [x] 在 Linear TIN-341 记录范围、分层、验收标准、测试与回滚方式
- [x] 完成状态层、交互层和布局层的版本化设计
- [x] 实现桌面端左右目录隐藏、正文扩展与偏好记忆
- [x] 完成构建和桌面/移动端浏览器验收
- [x] 完成 PR 门禁、合并和 Pages 生产复验

### 阶段 2.5：常见组态文章与动画 `complete`

- [x] 在 Linear TIN-343 记录范围、分层、验收标准和回滚方式
- [x] 完成文章层、独立演示层和公共步骤状态层的版本化设计
- [x] 编写并网点数据选择、光伏均衡、标幺值和过调检测正文
- [x] 实现四个支持自动播放、单步和复位的 Vue 动画
- [x] 完成完整构建和桌面/移动端浏览器验收
- [x] 完成 PR 门禁、合并和 Pages 生产复验

### 阶段 2.6：常见组态动效对齐 `complete`

- [x] 在 Linear TIN-344 记录动效范围、分层、验收标准和回滚方式
- [x] 明确只对齐第一版本地演示的动态表现，不改变 EnergyBook 既有主题外观
- [x] 恢复曲线数据通路、连续流动粒子和活动路径反馈
- [x] 将功率条、标幺值和过调状态改为平滑插值
- [x] 完成完整构建和桌面/移动端浏览器验收

### 阶段 2.7：补充光伏储能变量分配 `complete`

- [x] 在 Linear TIN-345 记录教学范围、分层、验收标准和回滚方式
- [x] 扩展版本化设计，明确光伏与储能的功率关系、SOC 约束和策略边界
- [x] 新增第五个独立 Vue 动画并注册到主题
- [x] 补充文章正文、公式、输入输出、处理流程和与光伏均衡的层级区别
- [x] 完成完整构建和桌面/移动端浏览器验收

### 阶段 2.8：补充运行模式与数据管理组态 `complete`

- [x] 在 Linear TIN-347 记录六个新增组态的范围、验收标准与回滚方式
- [x] 扩展版本化设计，明确运行模式、开关控制、事件录波与连续采样的职责边界
- [x] 新增离网控制、并网控制、`gridswitch_ctrl`、黑启动、`fault_record` 和 `sample_data_manager` 六个独立 Vue 动画
- [x] 补充文章正文、输入输出、触发条件、运行顺序与工程边界
- [x] 完成完整构建和桌面/移动端浏览器验收
- [x] 完成 PR 门禁、合并和 Pages 生产复验

### 阶段 2.9：统一十一张常见组态原版动画 `complete`

- [x] 在 Linear TIN-348 记录范围、分层、验收标准和回滚方式
- [x] 更新版本化设计，明确本地原版 HTML 是动画结构、路径、步骤与时序的权威来源
- [x] 将十一张原版动画及公共视觉样式纳入站点静态资源
- [x] 用统一 iframe 适配层替换十一份二次重绘组件，并同步主题与内容高度
- [x] 完成单步、复位、自动播放、普通/专注阅读、移动端和主题浏览器验收
- [x] 完成构建、PR 门禁、Pages 发布和生产复验

### 阶段 2.10：Codex 原版图形全局规则 `complete`

- [x] 在 Linear TIN-357 记录规则范围、验收标准和回滚方式
- [x] 新增版本化图形来源策略
- [x] 更新《内容与动画》指南和 README 内容规范入口
- [x] 完成构建、提交、发布与生产复验

### 阶段 2.11：IEC 104设计初衷概要文章 `complete`

- [x] 确认文章范围：业务视角、协议视角、Modbus 简单对比和一个贯穿案例
- [x] 编写简洁正文并加入工业控制导航
- [x] 制作并嵌入一份原版轻量动画，正文保留无 JavaScript 降级说明
- [x] 完成自动化检查、生产构建和桌面/移动端浏览器验收
- [x] 提交、推送、发布并复验正式站点

### 阶段 2.12：批阅功能健康检查 `complete`

- [x] 核对前端批阅入口、会话与批量提交实现
- [x] 运行批阅策略、网关、API 与发布器自动化测试
- [x] 验证生产站点入口与后端健康状态
- [x] 通过真实浏览器核对未登录入口与 OAuth 跳转
- [x] 追踪实际批阅执行并定位执行器来源白名单漏项
- [x] 汇总结论、局限与建议

### 阶段 2.13：修复批阅来源白名单漂移 `complete`

- [x] 在 Linear TIN-408 记录缺陷、范围、分层、验收与回滚
- [x] 写入版本化来源策略设计
- [x] 实现可注入的来源校验契约
- [x] 增加允许正式来源与拒绝未知来源的回归测试
- [x] 运行完整检查并验证恢复路径
- [x] 备份配置、部署执行器并完成无副作用生产验证
- [x] 同步 Linear 与仓库记录

### 阶段 3：核心知识与数据可视化 `pending`

- [ ] 编写能源基础、电力系统、化石能源、可再生能源、储能等核心内容
- [ ] 建立可复用动画与图表组件库
- [ ] 为每个可视化提供数据来源和无 JS 降级内容

### 阶段 3.1：能源控制器知识全景与掌握度 `complete`

#### 范围

- 新增一篇正式知识页，解释能源控制器七层全景。
- 依据现有研究成果标注“已了解、正在建立、需要补齐”，避免把资料收藏等同于工程掌握。
- 文章重点展开已经形成结构化理解的模块，并给出下一阶段补齐路线。
- 将页面接入现有导航，保留无 JavaScript 可读的正文。

#### 分层与依赖

1. Markdown 文章：叙事、证据边界、已掌握内容和学习路线。
2. 原版全景图：将当前 Codex 生成的全景图作为版本化静态演示，保持结构、内容和交互。
3. 嵌入层：沿用现有 iframe / 原版动画嵌入方式，负责主题与响应式适配。
4. VitePress 导航：只提供页面入口，不承载领域逻辑。

#### 验收标准

- [x] 新文章可从站点导航访问。
- [x] 页面明确说明评估口径，29 个模块计数不被解释为考试分数。
- [x] 原版全景支持状态筛选、模块详情查看、键盘操作和窄屏重排。
- [x] JavaScript 失效时，正文仍能读到主要结论、强项和补齐路线。
- [x] `npm run check` 通过。
- [x] 合并到主分支并完成 GitHub Pages 生产验证。

#### 回滚与兼容

- 不修改既有文章 URL、批阅接口或服务端部署。
- 回滚时删除新文章、静态演示和导航项即可。

### 阶段 3.2：LightGBM负荷预测动画文章 `complete`

#### 范围

- 新增一篇面向零基础读者的正式知识文章。
- 使用同一组六条负荷数据贯穿回归决策树、Boosting、GBDT、LightGBM和预测。
- 正文减少重复解释，由四份分步动画承担特征选择、阈值评分、逐树纠错和LightGBM优化过程。
- 将文章接入首页、顶部导航和“能源基础”侧边栏。

#### 分层与依赖

1. Markdown文章层：提供完整叙事、关键公式、边界和参考资料。
2. 原版动画层：保存四份已经确认的自包含HTML，不依赖站点Vue状态。
3. 嵌入层：iframe只负责加载、隔离和可访问标题，不重绘动画。
4. VitePress导航层：只提供页面入口，不承载算法逻辑。

禁止动画反向依赖Markdown或VitePress运行时；禁止在主题层复制算法数据与计算逻辑。

#### 验收标准

- [x] 一篇文章覆盖选定的A—G内容块，并保持同一案例与同一组数值。
- [x] 四份动画均可从文章加载并完成“下一步/上一步”交互。
- [x] JavaScript失效时，Markdown正文仍能读懂算法主线和关键计算。
- [x] 首页、顶部导航和侧边栏均可进入文章。
- [x] `npm run check`与`git diff --check`通过。
- [x] 仅提交本功能文件，不混入当前工作区既有未提交改动。
- [x] 推送后GitHub Pages部署成功，正式文章与四份动画返回HTTP 200。

#### 兼容与回滚

- 不修改既有文章URL、主题组件、在线批阅或服务端配置。
- 回滚时删除新增文章、四份动画和三个导航入口即可。
- 文章与动画均进入Git，部署机和构建产物不是唯一副本。

### 阶段 4：质量、发布与运营 `pending`

- [ ] 加入链接、Markdown、可访问性和构建检查
- [x] 完成 GitHub Pages 首次发布：<https://syrangg813s7vi-web.github.io/energy-handbook/>
- [x] 完成自定义域名 `energybook.foxtiny.com`（Cloudflare DNS、GitHub Pages 绑定与强制 HTTPS 均已完成）
- [ ] 建立版本、更新频率、数据时效性和反馈机制

## 当前关键决策

| 决策 | 状态 | 说明 |
|---|---|---|
| Markdown 作为主要内容源 | 已确定 | 易于维护、审核和迁移 |
| 图形与动画来源 | 已更新 | 文章图形以 Codex 直接生成并经本地确认的原始文件为权威源；EnergyBook 只做嵌入与适配，禁止二次近似重绘 |
| 静态站点生成器 | 已确定 | VitePress 1.6.4 + Vue 3 + Vite 6.4.3 |
| 在线批阅身份 | 已更新 | 公开可读；GitHub OAuth 登录后才显示并允许批阅，仅授权本仓库具有 push/maintain/admin 权限的用户 |
| 自动改稿执行 | 已确定 | `n8n.foxtiny.com`（SSH `molt`）提交 `codex cloud` 任务，检查后自动 PR/合并 |
| READY 差异发布 | 已更新 | Cloud Codex 当前只产出 READY diff；molt 使用仓库专属 Deploy Key 推送 `codex/review-*`，GitHub Action 建 PR、复检并自动合并 |
| 默认修改边界 | 已确定 | Markdown、VitePress 动画组件及 `public/demos`；禁止工作流、依赖和站点配置 |
| 登录会话网关 | 已部署 | `n8n.foxtiny.com/energy-review`；OAuth state + PKCE；实时校验 `energy-handbook` 的 `permissions.push`；一次性码 2 分钟，批阅令牌 15 分钟 |
| 批阅提交粒度 | 已更新 | 每次划线先加入浏览器会话内的批阅清单；用户确认后整批生成一个 Cloud 任务、一个受限分支和一个 PR |

## 待用户确认（不阻塞底座建设）

- [x] GitHub 归属与仓库：`syrangg813s7vi-web/energy-handbook`
- [x] 自定义域名：`energybook.foxtiny.com`
- 首批优先内容和主要读者（公众、学生、从业者或研究人员）
- 许可证选择（初始建议文本 CC BY 4.0，代码 MIT）

## 错误记录

| 日期 | 错误 | 尝试 | 解决方案 |
|---|---|---:|---|
| 2026-08-04 | 创建部署记录 PR 时 GitHub 连接器返回 MCP 传输错误 | 1 | 按发布技能回退到已鉴权 `gh pr create`，使用临时 Markdown 文件传入正确换行的 PR 正文 |
| 2026-08-04 | 首次同步部署证据时将 `findings.md` 的锚点误放在 `task_plan.md` 补丁段，整个补丁未应用 | 1 | 分别读取三份文件的真实上下文，拆分为独立文件补丁 |
| 2026-08-04 | 查询新 PR 时误用 `gh pr view --head`，该子命令不支持 `--head` | 1 | 改用分支名作为 `gh pr view` 位置参数 |
| 2026-08-04 | 修复分支 rebase 到最新 `origin/main` 时，三份追加式计划文件中的 `findings.md` 和 `progress.md` 在文件头冲突 | 1 | 业务代码无冲突；手工保留远程文章记录和本次批阅记录后继续 rebase |
| 2026-08-04 | 清理已验证的临时恢复目录时，安全策略拒绝 `rm -rf` | 1 | 改用可恢复的系统废纸篓移动，不重复删除命令 |
| 2026-08-04 | 恢复演练首次命令将尚未创建的随机临时目录设为 `workdir`，进程无法启动 | 1 | 改为从仓库启动，先 `mktemp` 后在同一 shell 中进入该目录 |
| 2026-08-04 | 远程 SQLite 查询的工作流 ID 字符串在两层 shell 引号中被解析为列名 | 1 | 改用 SQLite `char(...)` 常量构造 ID，避免重复相同引号命令 |
| 2026-08-04 | 首次同步健康检查计划时误判 `progress.md` 一级标题，整个补丁未应用 | 1 | 读取三份文件的真实锚点，用精确上下文重新应用 |
| 2026-07-15 | Playwright `fill` 将纯数字参数解析为 number，而命令要求 string | 1 | 改用元素级 `eval` 设置 range 值并派发原生 `input` 事件 |
| 2026-07-15 | VitePress 1.6.4 默认依赖 Vite 5.4.21，`npm audit` 报告 3 项开发服务器漏洞 | 1 | 通过 npm `overrides` 升级到与 Vue 插件兼容的 Vite 6.4.3，构建通过且审计归零 |
| 2026-07-15 | 专用执行器以低权限账号启动时 systemd 返回 `203/EXEC` | 1 | `/usr/local/bin/node` 实际指向不可穿越的 `/root/.nvm`；将 Node 二进制复制到受保护的 `/opt/energy-review-executor/bin` 并改用该路径 |
| 2026-07-15 | n8n 无副作用 Webhook 测试返回 `ERR_INVALID_HTTP_TOKEN` | 1 | Header Auth 的 Name 字段误填为带空格的凭据名称；改为合法 Header 名 `x-review-executor-token`，并用无换行方式重贴令牌 |
| 2026-07-15 | 执行器正确返回 400，但 n8n 脱敏响应误映射为 502 | 1 | n8n 2.19.5 的 HTTP Request 错误状态位于 `$json.error.status`；响应表达式增加该字段并保留旧版兼容回退 |
| 2026-07-15 | Cloudflare One 控制台进入 `/one/overview` 后持续停留在 Loading | 2 | 已刷新并改用可视化浏览器确认；账户仍显示 Zero Trust “Get started”，需要完成组织初始化后才能创建 Access 应用 |
| 2026-07-15 | Cloudflare Zero Trust 的 `/one/`、`/one/overview`、`/one/get-started` 均直接返回 JSON `{}` | 4 | 官方状态正常且新旧入口结果相同，判定为账户/新版控制台初始化故障；经用户同意改用无仓库 scope 的 GitHub OAuth 身份登录 |
| 2026-07-15 | 登录网关启用 `MemoryDenyWriteExecute` 后 Node.js V8 以 `TRAP` 退出 | 1 | 移除与 JIT 不兼容的单项限制，保留只读文件系统、无提权、能力清空等 systemd 隔离 |
| 2026-07-15 | `systemctl reload nginx` 因既有 PrivateTmp mount namespace 缺少 `/tmp` 而失败 | 1 | 配置已先通过 `nginx -t`；改由现有 master 进程执行 `nginx -s reload`，避免中断服务 |
| 2026-07-15 | n8n 重启后健康接口先于活动工作流注册完成，首次 Webhook 检查返回 404 | 1 | 等待启动日志确认工作流 Activated 后重试，错误链路返回预期 HTTP 400 |
| 2026-07-15 | 推送部署提交时远端 `main` 已新增能量管理文章提交，non-fast-forward 被拒绝 | 1 | 检查远端改动无重叠后 rebase 到 `origin/main`，重新运行完整测试与构建 |
| 2026-07-15 | Cloudflare 域名页报刷新错误，直接打开 `/dns/records` 仍只返回 `{}` | 3 | 当前无 DNS 写权限 CLI 会话；暂将 Pages 构建基路径设为 `/energy-handbook/`，保证备用地址功能完整，保留 CNAME 待控制台恢复 |
| 2026-07-16 | Wrangler OAuth 没有 DNS Write 权限 | 2 | 从 2026-06-02 本地历史会话恢复用户创建的、限定 `foxtiny.com` 的 DNS Edit Token，经验证仍有效，使用 Cloudflare API 创建 DNS-only CNAME |
| 2026-07-15 | Cloud Codex 任务完成后没有创建分支或 PR | 1 | 实测原生集成只保留 READY diff；改由低权限执行器应用 diff，并用单仓库 Deploy Key 推送受限分支 |
| 2026-07-15 | 发布器首次应用 READY diff 后误报没有修改 | 1 | `codex cloud apply` 默认将差异放入暂存区；文件检测改为 `git diff --name-only HEAD`，同时覆盖暂存与未暂存修改 |
| 2026-07-15 | PR #3 最后一步启用 auto-merge 返回 `Pull request is in unstable status` | 1 | 同一 push job 在结束前等待自身检查会形成循环；门禁均已在前序步骤通过，改为最后一步直接 squash merge |
| 2026-07-16 | Playwright `run-code` 用两个顶层语句注入本地测试会话时报语法错误 | 1 | 改为单个 async IIFE，保证 CLI 接收一个完整表达式 |
| 2026-07-16 | 重启批阅执行器后立即探测健康端口返回连接失败 | 1 | systemd 已 active 但 Node 尚在绑定端口；等待启动日志后重试，健康接口返回 200 |
| 2026-07-17 | 首次链接检索命令包含冲突的 shell 引号，zsh 报 `unmatched quote` | 1 | 将 Markdown 与 HTML 检索拆成两个简单 `rg` 命令后成功执行 |
| 2026-07-17 | Web open 对 GitHub Pages 与自定义域名返回 safe-open 内部错误 | 1 | 改用 curl 批量状态检查和 Playwright 真实点击验证，不重复相同调用 |
| 2026-07-17 | zsh 链接状态循环使用保留只读变量 `status` | 1 | 变量改名为 `http_code` 后成功取得演示文件状态 |
| 2026-07-20 | Playwright CLI 使用不存在的 `goto` 子命令 | 1 | 按技能命令表改用 `open <url>` 导航，后续快照与验收正常 |
| 2026-07-20 | 生产测量的 Playwright `run-code` 表达式多写一个右括号 | 1 | 去掉非必要字段并用单一合法表达式重试，成功取得布局与持久化结果 |
| 2026-07-22 | iframe 高度测量先后读取 `documentElement.scrollHeight`、`body` 高度；两者都会吸收 iframe 视口高度，形成持续增长 | 2 | 改为测量 `body` 中实际可见子元素的最下边界并补齐内边距，十一张图稳定在 408–602 px |
| 2026-07-22 | 自动播放验收准备点击“暂停”时，动画已自行播放至末步，旧状态定位器数量变为 0 | 1 | 重新读取当前 DOM，确认十一张均到达末步且按钮自动恢复“自动演示”，随后按新状态逐张复位 |
| 2026-07-22 | 桌面任务环境默认 `PATH` 不含 npm，首次完整检查的嵌套 `npm` 脚本找不到命令 | 1 | 将已安装的 Homebrew Node/npm 目录显式加入 `PATH` 后重新运行完整检查 |
| 2026-07-22 | 桌面任务环境默认 `PATH` 不含 GitHub CLI，首次创建 PR 命令找不到 `gh` | 1 | 定位已安装的 GitHub CLI 并使用绝对路径重试 |
| 2026-07-22 | Pages 部署后用浏览器新标签复验生产页时连接超过工具时限 | 1 | 不重复超时导航；采用已通过的本地完整交互矩阵，并以生产文章、十一份 HTML 和公共 CSS 全部 HTTP 200 作为部署复验依据 |
| 2026-07-23 | 首次同步本阶段三份计划文件时误判 `progress.md` 一级标题，补丁未应用 | 1 | 读取文件实际标题后拆分精确上下文重新应用 |
| 2026-07-23 | 浏览器接口不支持等待 `networkidle`，首次页面等待调用未执行 | 1 | 页面已完成导航，改用DOM快照和目标元素读取确认加载状态 |
| 2026-07-23 | 浏览器对双层沙箱 iframe 内按钮的常规定位点击被内层按钮拦截 | 2 | 重新读取可见DOM，使用唯一节点ID完成真实点击并验证每一步状态 |
| 2026-07-23 | 网页读取接口将 EnergyBook 自定义域名判为不可安全打开 | 1 | 沿用仓库既有复验策略，改用HTTP状态检查和真实浏览器 |
| 2026-07-23 | 正式文章的真实浏览器导航超过30秒工具时限并重置会话 | 1 | 不重复相同超时路径；Pages部署成功，文章与动画两个正式地址均独立返回HTTP 200 |
| 2026-07-23 | Linear 工作区已达到免费版 issue 上限，创建本功能事项失败 | 1 | 保留完整范围、验收、回滚和完成定义于仓库计划；继续实施，并在发布完成后记录 Linear 同步受限 |
| 2026-07-28 | 当前会话未提供可调用的 Linear 工具 | 1 | 沿用仓库计划文件记录本功能；不把发布阻塞在外部追踪工具连接上 |
| 2026-07-28 | 检查工作流的 shell 输出标题以 `---` 开头，`printf` 将其误判为选项 | 1 | 后续使用固定格式串 `printf '%s\n'` 输出标题，不重复该写法 |
| 2026-07-28 | 首次提交前检查发现文章末尾多一个空行，但命令未使用失败即停，随后仍创建了提交 | 1 | 删除多余空行、重新运行暂存区检查并修订提交；后续发布串行门禁使用失败即停 |
| 2026-07-28 | PR创建后立即执行`gh pr checks --watch`，检查任务尚未注册并返回“no checks reported” | 1 | 不重复立即查询；先读取PR与Actions状态，等待工作流出现后再监控 |
| 2026-07-28 | `gh pr merge`试图在合并后切换本地主分支，被工作区既有未提交计划文件阻止 | 1 | 不暂存、不丢弃用户改动；先查询PR状态，若尚未合并则改用GitHub远端合并接口 |
| 2026-07-28 | 桌面任务环境默认`PATH`不包含`curl`，首次正式地址检查未执行 | 1 | 使用系统绝对路径`/usr/bin/curl`完成同一只读检查 |
| 2026-07-28 | 正式地址均返回200后，正文内容管道因默认`PATH`不包含`rg`提前关闭 | 1 | 保留已完成的HTTP结果，正文检查改用`/opt/homebrew/bin/rg`并避免重跑全部地址 |
| 2026-07-23 | 远端 `main` 比本地多 13 个提交，带计划文件改动执行 `rebase --autostash` 后三份计划文件发生文本冲突 | 1 | 保留远端新增阶段与 IEC 104 记录，同时合并本次全景计划、发现和进度；未丢弃任一侧内容 |
| 2026-07-23 | 一条 shell 命令连续启动三个 Playwright CLI 子进程时，新守护进程误判浏览器仍被占用 | 1 | 停止批量启动，改为单条 CLI 命令顺序执行；重新打开页面后完成移动端与控制台验证 |
| 2026-07-23 | `check-complete.sh` 不识别本仓库使用反引号状态的阶段标题，报告 0 个阶段 | 1 | 不改动仓库既有计划格式；人工核对阶段 3.1 已标记 `complete` 且六项验收全部勾选，并以构建、浏览器和生产证据确认完成 |

## 阶段 3.4：中国2021—2025年分电源发电量趋势文章 `complete`

### 范围

- 新增一篇行业洞察文章，统一呈现2021—2025年全国总发电量以及火电、水电、核电、风电、太阳能发电量。
- 使用表格比较绝对量、发电结构、同比增速、四年累计变化、增量贡献和火电/风光关系，并给出面向普通读者的结论。
- 数据以国家统计局年度《国民经济和社会发展统计公报》为主，补齐官方原始链接、访问日期和统计口径说明。
- 将文章接入行业洞察导航，提交、发布并复验正式站点。

### 依赖与单向边界

1. Markdown文章层：保存数据表、计算方法、结论、口径和参考来源。
2. VitePress导航层：只提供文章入口，不复制数据和结论。
3. GitHub Pages发布层：只消费Git中的版本化内容。

禁止导航或主题组件反向持有文章数据；禁止把开发机缓存、构建产物或搜索摘要作为唯一证据源。

### 验收标准

- [x] 五年各分项数据与官方年度公报一致，合计、占比、增速和增量贡献可复算。
- [x] 明确区分发电量、全社会用电量和装机容量，并说明火电口径边界。
- [x] 每个年度提供国家统计局官方原始链接与访问日期。
- [x] 文章可从行业洞察导航访问，既有URL和主题功能不受影响。
- [x] `npm run check`与`git diff --check`通过。
- [x] 提交、推送、PR合并、Pages部署和正式页面HTTP 200复验完成。

### 兼容与回滚

- 不修改既有文章URL、主题组件、在线批阅或服务端配置。
- 回滚时删除新增文章并移除两个行业洞察导航入口即可。
- 权威来源、文章和恢复步骤均进入Git；RPO以最后一次推送提交为界，静态站目标RTO为1小时内重新构建发布。

### 追踪

- Linear：TIN-405，状态为 In Progress。
- 分支：`codex/tin-405-china-generation-trends`，独立工作树隔离当前仓库已有未提交改动。

### 完成证据

- PR #38通过仓库门禁并完成squash合并，`main`合并提交为`1dc35d9`。
- GitHub Pages运行`30908833205`的build和deploy均成功。
- 正式文章与首页均返回HTTP 200；正式HTML包含文章路由、标题和国家统计局来源。
- 本地浏览器在1280 px桌面与390×844移动端完成验收，无页面级横向溢出、错误覆盖层或控制台警告。

### 验证备注

- `npm ci`报告1项现存中等级开发依赖风险，本功能未修改依赖或锁文件，后续应由独立依赖维护事项处理。
- GitHub Actions提示Node.js 20 actions正在被强制运行于Node.js 24；本次部署成功，但工作流actions版本应在独立维护中升级。
- `agent-browser`命令在当前环境不存在；按浏览器验证技能的失败处理要求，改用已配置的Playwright CLI完成等价验证，没有把工具缺失误判为页面失败。
# MHS 问题与解决方案文章发布（2026-08-31）

## Goal

发布一篇只聚焦 MHS 要解决的问题与解决方案的中文技术文章，并完成导航接入、构建、发布和生产验证。

## Phases

### Phase 1: 确认内容范围与原始证据

**Status:** complete

### Phase 2: 编写文章与接入站点

**Status:** complete

### Phase 3: 构建和页面验证

**Status:** complete

### Phase 4: 发布与生产验证

**Status:** complete

## Errors Encountered

| Error | Attempt | Resolution |
|---|---|---|
| 内容断言脚本检查英文 `research preview`，而正文采用中文“研究预览”，因此误报缺失 | 1 | 将断言改为检查正文实际使用的“研究预览”；不修改正确的文章内容 |

---

# CIGRE 2026 厂商现场观察发布（2026-09-05）

## Goal

将已整理的 CIGRE 2026 厂商现场照片与主题分析发布为 EnergyBook 原生行业洞察文章。Linear：当前环境无可用连接器，先以仓库规划文件和 GitHub PR 记录状态。

## Scope and acceptance

- 新增一篇原生 EnergyBook 文章，按“主题—分析结论—厂商照片”组织。
- 四个主题分别为数字化转型、IEC 61850、华为电力军团与其他；厂商范围限定为 ABB、ETAP、GE Vernova、Hitachi Energy、南瑞继保、SEL、Siemens 和华为。
- 162 张已优化现场照片全部进入版本库，并按主题、厂商分组展示；图片按需加载且可展开查看。
- 新文章接入首页、顶部导航和侧边栏，不破坏已有路由或内容。
- `git diff --check`、自动化测试、VitePress 生产构建、PR/Pages 发布与生产 URL 检查通过。
- 回滚只需移除新增文章、图库组件、照片资产和三个入口，不影响既有页面。

## Layer and dependency boundaries

- 内容层：Markdown 负责主题与分析结论，不依赖图库实现细节。
- 展示层：Vue 图库组件只消费版本化照片清单，不修改全站状态。
- 资产层：静态照片位于 `docs/public/visuals/`，由可复现脚本生成清单；上层只使用公开路径。
- 禁止依赖：文章不得读取本机路径；图库不得依赖外部运行时、第三方图片服务或私有 API。

## Phases

### Phase 1：固定设计与资产边界

**Status:** complete

### Phase 2：文章、图库与站点入口

**Status:** complete

### Phase 3：构建与验收

**Status:** complete

### Phase 4：发布与生产复验

**Status:** complete

## Errors encountered

| Error | Attempt | Resolution |
|---|---|---|
| 本地抽查时手工猜测了一条华为照片文件名，单条请求返回 404 | 1 | 从版本化目录读取真实路径后返回 HTTP 200；随后并行检查全部 162 条实际路径，均可访问 |
| `gh pr merge --delete-branch` 在 GitHub 合并成功后尝试本地切换 `main`，但该分支已被另一工作树占用 | 1 | 通过 PR API 确认合并提交 `9a1ae7c` 已生成；不切换或改写任何已有工作树，继续验证 Pages 部署 |
| VitePress 把 Markdown 形式的 PPTX 链接判定为死链，尽管文件已在 `docs/public/downloads/` | 1 | 改用带 `download` 属性的原生 HTML 附件链接，使二进制下载资源绕过页面路由检查，并重新执行完整构建 |

## Follow-up: update analysis and attach the presentation

### Scope and acceptance

- Expand the GE Vernova, Siemens, ABB and Huawei conclusions without changing the article route or gallery classification.
- Remove visible photo-number labels beneath gallery images and in the lightbox.
- Publish the validated 27-page, 162-photo v4 PPTX as a versioned EnergyBook download.
- Preserve all 162 photos and pass the existing test, production-build, PR and Pages workflow.

### Phases

- [x] Update the article conclusions and gallery presentation.
- [x] Add the full-quality PPTX and validate the local production download.
- [x] Merge through PR and verify the Pages deployment and production attachment.

# MHS 文章案例增补（2026-08-31）

## Goal

在现有 MHS 问题与解决方案文章中补充 Anthropic 原始发布的实际案例，用案例验证前文架构，同时保留 PoC、自报结果和当前模型能力边界。

## Scope and acceptance criteria

- 覆盖 Genentech、UW Baker/Pinglay、CMU、HHMI Janelia、QuEra、Tetsuwan 六组原始案例。
- 每个案例说明设备组合、MHS 发挥的作用、结果和限制，不扩展为合作伙伴宣传名单。
- 案例数据以已归档的 2026-08-27 Anthropic 原始页面为准；Tetsuwan 页面版本差异需注明。
- 不改变文章 URL、首页和导航；通过现有测试、构建、PR 和 Pages 流程发布。

## Phases

### Phase 1: 固定案例数据与文章插入结构

**Status:** complete

### Phase 2: 编写六组案例与证据边界

**Status:** complete

### Phase 3: 构建和页面验证

**Status:** complete

### Phase 4: 发布并验证生产页面

**Status:** complete

## Errors Encountered

| Error | Attempt | Resolution |
|---|---|---|
| 一次 `apply_patch` 对同一文件声明了多个独立 Update 操作，校验拒绝整个补丁 | 1 | 合并为每个文件单一 Update 操作后重新应用，未产生部分写入 |

---
# MHS 文章标题润色（2026-08-31）

## Goal

将文章标题改为更自然、准确且能体现 MHS 名称与“连接＋控制”双重价值的表达，保持正文、URL和导航兼容。

## Phases

### Phase 1: 确定并更新标题与页面元数据

**Status:** complete

### Phase 2: 构建验证

**Status:** complete

### Phase 3: 发布和生产验证

**Status:** complete

## Errors Encountered

| Error | Attempt | Resolution |
|---|---|---|

---
# MHS 文章加入 Anthropic 演示视频（2026-08-31）

## Goal

在现有文章最后增加 Anthropic 原始公告中的全部十段演示视频，按主题分组、按需加载，并验证桌面/移动页面和远程媒体可播放性。

## Scope and acceptance criteria

- 视频使用 Anthropic 公告实际引用的 Sanity CDN 原始地址，不上传约780 MB媒体到Git仓库。
- 覆盖工作原理、UW/CMU、Janelia、QuEra、Tetsuwan共10段视频。
- 所有播放器设置 `preload="none"` 与 `playsinline`，避免打开文章即下载大型媒体。
- 视频有中文标题和文字降级；URL、正文和现有导航不变。
- 六组早期案例改为统一四列表格，并保留关键结果、失败边界和证据限制。
- 通过构建、远程媒体HTTP检查、PR/Pages发布和生产HTML验证。

## Phases

### Phase 1: 固定十段视频清单与加载策略

**Status:** complete

### Phase 2: 增加最终视频章节与响应式样式

**Status:** complete

### Phase 3: 构建、媒体和页面验证

**Status:** complete

### Phase 4: 发布并验证生产页面

**Status:** complete

## Errors Encountered

| Error | Attempt | Resolution |
|---|---|---|
| 首次按TSV字段批量探测视频时，空`alt`列使shell字段错位，`curl`收到空URL | 1 | 改为从文章直接提取10个唯一媒体URL；全部以HTTP 206响应并返回正确视频类型 |
| 正式页检查脚本使用zsh只读变量`status` | 1 | 改名为任务专用变量`prod_status`后重跑；生产页各项检查全部通过 |

---
# 软件定义 PAC 架构分析发布（2026-09-04）

## Goal

将现场资料整理为可公开、可追溯的 EnergyBook 行业洞察，配套原创架构图并发布到正式站点。Linear：TIN-636。

## Scope and acceptance

- 新增软件定义 PAC 文章、原创架构图、首页入口、顶部导航和侧边栏入口。
- 区分 FIH 目标、虚拟化手段、厂商主张、现场观察与公开事实。
- 不公开转载现场厂商照片，不新增运行时依赖。
- `git diff --check`、`npm run check`、桌面/移动浏览器和生产 URL 验证通过。
- 回滚只需删除新增文章、图形和入口，不影响既有 URL。

## Phases

### Phase 1：资料与设计边界

**Status:** complete

- 确认 CIGRE、Siemens Energy、GE Vernova 与 LF Energy 一手资料。
- 固化分层内容、证据边界、素材策略和回滚方式。

### Phase 2：文章与原创图形

**Status:** complete

- 编写正文和原创分层架构图。
- 接入首页、顶部导航和侧边栏。

### Phase 3：构建与浏览器验收

**Status:** complete

- 执行自动化测试、生产构建和桌面/移动页面检查。

### Phase 4：发布与生产复验

**Status:** complete

- 通过 PR 合并并确认 GitHub Pages、canonical 和 sitemap。

---
